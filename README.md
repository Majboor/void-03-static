# VOID/03 — Modular Transit Shell (static)

Faithful static copy of the VOID/03 aura.build template — self-contained `index.html`
(inline CSS + JS; imagery from the public jsDelivr CDN). Renders anywhere, unlike the
original aura SPA whose content is resolved from aura's backend.
